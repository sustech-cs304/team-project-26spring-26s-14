# Design Report — 校园社交市场 (Campus Market)

> Team ID: 14  
> Sprint: 1  
> 提交日期: 2026-05-28

---

## Part I. Architectural Design

### 1. 架构概览

系统采用 **前后端分离 + BaaS (Backend-as-a-Service)** 架构。前端为单页应用 (SPA)，所有后端逻辑（认证、数据库、存储、实时推送）由 Supabase 托管服务承担，团队无需自建服务器。

```
┌─────────────────────────────────────────────┐
│              Client Browser                 │
│  ┌─────────┐ ┌─────────┐ ┌─────────────┐   │
│  │  Home   │ │   Map   │ │    Chat     │   │  ... 14 Views
│  │ (Vue+TS)│ │ (Vue+TS)│ │   (Vue+TS)  │   │
│  └────┬────┘ └────┬────┘ └──────┬──────┘   │
│       └─────────────┬─────────────┘        │
│              Pinia (Global State)           │
│  ┌──────────────────┴──────────────────┐    │
│  │      Vue Router 4 (History Mode)    │    │
│  └──────────────────┬──────────────────┘    │
└───────────────────────┼──────────────────────┘
                        │ HTTPS / REST / WebSocket
┌───────────────────────┼──────────────────────┐
│         Supabase Cloud│                      │
│  ┌────────────────────┴──────────────────┐    │
│  │         PostgreSQL Database           │    │
│  │  profiles · items · messages · reviews│   │
│  │  favorites · wishlist · notifications │    │
│  │  auth.users (managed by GoTrue)     │    │
│  └───────────────────────────────────────┘    │
│  ┌─────────────┐ ┌─────────────┐ ┌────────┐ │
│  │ Auth (JWT)  │ │Storage (Img)│ │Realtime│ │
│  └─────────────┘ └─────────────┘ └────────┘ │
│  ┌────────────────────────────────────────┐ │
│  │  Triggers: handle_new_user()           │ │
│  │            update_reputation_score()   │ │
│  │            check_wishlist_matches()    │ │
│  └────────────────────────────────────────┘ │
└─────────────────────────────────────────────┘
           │
    ┌──────┴──────┐
    │ Tencent Map │
    │   API GL    │
    └─────────────┘
```

### 2. 组件职责与交互

| 组件/层级 | 职责 | 与其他组件的交互 |
|-----------|------|------------------|
| **Views (14 pages)** | 承载业务页面，处理用户输入与展示 | 调用 API Layer 获取数据；读取 Pinia Store 获取全局状态 |
| **Components (2)** | 复用弹窗：评价弹窗、编辑资料弹窗 | 由 Views 通过 props/events 调用；内部调用 API Layer |
| **API Layer (7 modules)** | 封装所有 Supabase 调用，对外暴露纯函数 | 被 Views 和 Components 调用；依赖 supabase.ts 客户端 |
| **Pinia Store (user)** | 全局用户状态：当前用户、profile、未读计数 | 在 App.vue 初始化时调用 `init()`；提供 `signUp/signIn/signOut` 方法供 AuthView 使用 |
| **Router** | 前端路由映射，控制页面跳转与权限 | 读取 Store 判断登录状态；支持动态参数（`:id`, `:userId`） |
| **Supabase Client** | 单一实例，配置 URL 与 Anon Key | 被 API Layer 所有模块共享；自动处理 JWT 附带头 |
| **Triggers** | 数据库级业务逻辑：自动创建 profile、计算信誉分、愿望匹配通知 | 由 PostgreSQL 在 INSERT/UPDATE 后自动触发，应用层无感知 |

### 3. 架构决策理由

- **选择 Vue 3 + Vite**：组合式 API (`<script setup>` + `ref/computed`) 适合复杂状态逻辑（如聊天轮询、地图聚合）；Vite 提供极速冷启动与 HMR，相比 Webpack 减少 70% 以上构建等待时间。
- **选择 Supabase BaaS**：课程项目周期短（2 个 Sprint，约 10 周），自建后端（Node.js + Docker + Nginx）会引入过多运维负担。Supabase 提供开箱即用的 Auth、Database、Storage 和 RLS，使团队能将 80% 精力集中在业务功能而非基础设施。
- **选择腾讯地图 GL**：国内网络环境下 Google Maps 不稳定；腾讯地图 GL 版本基于 WebGL 渲染，性能显著优于 JSAPI v2，且支持自定义 DOM 覆盖物（实现聚合数字标签）。
- **History Mode Router**：URL 更美观（无 `#` 哈希），利于 SEO 与分享；配合 Vercel `vercel.json` rewrite 规则可支持直接访问子路径（如 `/items/xxx`）不 404。
- **RLS + 触发器**：将权限控制下沉到数据库层，即使前端被绕过，数据仍安全；触发器保证信誉分计算与愿望匹配的原子性，避免并发竞态。

### 4. 隐藏假设与约束

- **语言假设**：用户主要使用中文，搜索匹配使用 PostgreSQL `ilike`（不区分大小写与重音），但不支持中文分词（如"自行车"无法匹配"单车"）。
- **隐私假设**：图片存储在 Supabase Storage 的 Public bucket，假设校园内二手交易图片（教材、日用品）不涉及高敏感隐私；如需隐私保护，应改为 Signed URL + 私有 bucket。
- **实时性假设**：聊天 10 秒轮询在 MVP 阶段可接受（校园交易非高频即时通讯），但并发 >100 用户时建议升级为 Supabase Realtime（WebSocket）。
- **用户范围假设**：所有用户均为南方科技大学师生，校园邮箱域名白名单（`sustech.edu.cn`, `mail.sustech.edu.cn`）已硬编码在 `signUp` 方法中，未来扩展其他学校需重构为配置表。
- **坐标精度**：地图聚合按坐标小数点后 4 位分桶（约 10 米范围），假设校园内同一宿舍楼/食堂发布的商品视为"同地点"。

---

## Part II. UI Design

> 以下为主要功能页面的 UI 设计说明。设计遵循"移动优先、桌面适配"原则，使用 Tailwind CSS 实现响应式布局。所有设计在 Sprint 1 期间完成，并作为 Sprint 2 前端实现的指导文档。

### 1. 首页 / 商品列表 (HomeView / ItemListView)

**布局结构**：
- **顶部固定导航栏**（h-16，白色背景，底部阴影）：左侧 Logo + 应用名；中间搜索框（ rounded-full，带放大镜图标）；右侧消息图标（带红点计数）、通知图标、用户头像（圆形，点击下拉菜单）。
- **二级筛选栏**（h-12，浅灰背景）：横向滚动类型标签（"全部"、"出售"、"技能交换"、"失物招领"），当前选中项为蓝色底白字；右侧价格区间输入框（min-max）和排序下拉框（最新/价格升序/价格降序）。
- **主体商品网格**：桌面端 4 列 (`grid-cols-4`)，平板 2 列，手机 1 列；卡片间距 `gap-4`。

**商品卡片 (Card) 设计**：
- 顶部：首图区域（`aspect-[16/10]`，圆角 `rounded-lg`，object-cover）。
- 右上角悬浮：收藏按钮（心形图标，未收藏时描边灰色，已收藏时填充红色）。
- 中部：标题（单行截断 `truncate`，font-semibold）；价格（大字号、红色 `text-red-500`）；类型标签（小药丸形状 `rounded-full px-2 py-0.5`，不同颜色区分类型）。
- 底部：卖家昵称（灰色小字）+ 信誉分（黄色星星图标 + 数字）。

**空状态 (Empty State)**：
- 居中插图（使用 SVG 或在线插图库，如 undraw）。
- 文案："暂无商品，去发布一个吧"（灰色）。
- 操作按钮：蓝色主按钮 "立即发布"，点击跳转 `/publish`。

### 2. 校园地图 (MapView)

**布局结构**：
- **桌面端**：左右分栏。左侧 30% 宽度为可折叠商品列表（按发布时间排序，卡片紧凑版）；右侧 70% 为腾讯地图画布。
- **移动端**：地图占满全屏；底部为抽屉式列表（初始高度 30%，可上滑展开至 70%）。

**地图交互设计**：
- **默认视角**：以南科大中心坐标（22.5943, 113.9962）为中心，缩放级别 16，显示校园道路与建筑轮廓。
- **标记样式**：单个商品为蓝色定位图标（`marker-icon`）；同一坐标（10 米范围内）多个商品聚合为圆形蓝色标签，白字显示数量（如 "3"）。
- **点击聚合标签**：弹出气泡卡片列表（`max-h-60 overflow-y-auto`），每项显示缩略图 + 标题 + 价格，点击跳转详情。
- **点击单个标记**：直接弹出信息窗（InfoWindow），显示商品标题与价格，带"查看详情"按钮。
- **图例 (Legend)**：右下角固定卡片，说明蓝色标记 = 商品位置，数字圆圈 = 聚合数量。

### 3. 实时聊天 (ChatView / ChatsView)

**会话列表 (ChatsView)**：
- **桌面端**：左侧边栏（宽 320px，白色背景，右边框）。列表项：左侧圆形头像（40px）、中间上方对方昵称（font-medium）、中间下方最后消息预览（灰色小字，单行截断）、右侧上方时间戳（灰色极小字）、右侧下方未读红点（红色圆形，白色数字）。
- **当前选中会话**：浅蓝背景高亮。
- **移动端**：全屏列表，点击进聊天详情。

**聊天窗口 (ChatView)**：
- **顶部栏**：返回箭头（移动端）、对方昵称（居中）、关联商品卡片（如果从商品详情进入，显示缩略图 + 标题，可点击跳转）。
- **消息区**：flex-col-reverse 或正常流，消息气泡：
  - 己方：绿色背景 (`bg-green-500`)，白色文字，圆角右下直左下圆，靠右对齐。
  - 对方：白色背景，深灰文字，圆角左下直右下圆，靠左对齐。
  - 时间戳：每条消息下方显示 `HH:MM`，灰色极小字。
- **底部输入区**：固定在底部。左侧文本输入框（多行，自动增高，max-h-32）；右侧发送按钮（纸飞机图标，蓝色圆形）。
- **空状态**："开始聊天吧" + 握手插图。

### 4. 商品详情 (ItemDetailView)

**布局结构**：
- **顶部图片画廊**：横向滑动轮播（`overflow-x-auto snap-x`），每张图宽 100% 容器，圆角。底部指示器：小圆点，当前项蓝色，其他灰色。
- **收藏按钮**：绝对定位在图片区域右上角，圆形白底阴影，心形图标。
- **信息区**：
  - 标题（h1，大字号，font-bold）
  - 价格 + 类型标签（横向排列）
  - 发布时间（灰色小字，相对时间如"2 小时前"）
  - 地点名称（带定位图标）
- **卖家卡片**（灰色浅底卡片）：左侧大头像（56px）；中间昵称 + 学号（灰色）+ 信誉分（5 星可视化，黄色星星）；右侧"查看主页"文字按钮。
- **操作区**："联系卖家"主按钮（宽 100%，蓝色，大圆角，白色文字）。
- **描述区**：标题"商品描述"（font-semibold），正文（灰色，行高宽松）。
- **评价入口**：标题"卖家评价"，列表项：评价者头像 + 昵称、星级（1-5 星）、评论内容、时间。

### 5. 个人主页 (ProfileView)

**布局结构**：
- **顶部资料卡**（渐变背景或白色）：大头像（80px，圆形，居中）；昵称（大字号，居中）；学号 + 专业（灰色小字）；信誉分（超大数字 + "分" + 5 星可视化）。
- **标签页切换 (Tabs)**：三栏等宽，下划线指示器。"在售商品"、"已下架"、"收到的评价"。
- **在售商品网格**：与首页卡片一致，但增加：
  - 卖家视角：卡片下方显示"编辑"和"下架"文字按钮（蓝色/红色）。
  - 空状态："还没有在售商品"。
- **已下架列表**：卡片透明度降低（`opacity-60`），显示"重新上架"按钮。
- **评价列表**：横向卡片，左侧评价者头像 + 昵称，中间星级 + 评论，右侧时间。

### 6. 发布商品 (PublishView)

**布局结构**：
- **表单区**（垂直排列，gap-4）：
  - 标题输入（`input`，placeholder "例如：二手线性代数教材"）
  - 类型单选：三选项横向排列，选中为蓝色边框 + 蓝色圆点。
  - 价格输入（`input type=number`，前缀 "¥"，仅当类型为"出售"时显示）。
  - 描述文本域（`textarea`，4 行，placeholder "描述一下商品的新旧程度、交易要求..."）。
- **图片上传区**：
  - 6 个占位方框（`aspect-square`，虚线边框，灰色）。
  - 已上传方框显示缩略图 + 右上角删除按钮（X）。
  - 上传中显示进度条或旋转 loading。
- **地点选择区**：
  - 下拉框：常用地点（一丹图书馆、中心食堂等 6 个选项）。
  - "自定义地点"按钮：点击唤起腾讯地图选点弹窗（地图 + 搜索框 + 确认按钮）。
- **底部提交栏**：固定在底部，显示"发布"主按钮。未填完必填项（标题、类型）时按钮禁用（灰色）。

---

*注：以上 UI 设计在 Sprint 1 期间完成，作为 Sprint 2 前端实现的基准文档。实际实现中因腾讯地图聚合 API 对自定义 DOM 覆盖物的支持限制，对聚合标签的视觉表现进行了微调（使用圆形 div 替代原生 marker label）。*
